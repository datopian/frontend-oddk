<ul>
  {% for page in result.posts  %}
  <li class="mt-10">
    <div>
      <h3 class="text-2xl font-semibold">
        <a class="text-primary" href="/{{ page.slug }}">{{ page.title | safe}}</a>
      </h3>
      <div class="leading-relaxed mt-2">
        <p>
          {{ page.excerpt | safe | truncate(200) or page.content | safe | truncate(200) }}
          <a href="/{{ page.slug }}" class="hover:underline">læs mere</a>
        </p>
      </div>
    </div>
  </li>
  {% endfor %}
</ul>
